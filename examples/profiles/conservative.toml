# Conservative Profile
#
# Maximum safety for unfamiliar repositories, audits, and reviews.

model = "gpt-5.5"
model_reasoning_effort = "low"
approval_policy = "untrusted"
sandbox_mode = "read-only"

# Usage:
#   codex --profile conservative
#
# Read-only sandboxing prevents workspace edits and keeps network blocked.
