# Development Profile
#
# Balanced configuration for everyday local documentation and code work.

model = "gpt-5.5"
model_reasoning_effort = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

# Usage:
#   codex --profile development
#
# If this model is not enabled for your account, keep the profile shape and
# replace only the model value.
